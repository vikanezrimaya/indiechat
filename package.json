{
  "name": "nodeapp",
  "version": "0.0.0",
  "description": "",
  "main": "app.js",
  "bin": "app.js",
  "scripts": {
    "start": "node app.js",
    "build": "mkdir -p dist && browserify static/main.js -o dist/bundle.js",
    "prestart": "mkdir -p dist && browserify static/main.js -o dist/bundle.js -d",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.21.1",
    "browserify": "^16.5.0",
    "dialog-polyfill": "^0.5.0",
    "ejs": "^2.7.1",
    "express": "^4.17.1",
    "mf-obj": "^2.1.0",
    "querystring": "^0.2.0",
    "socket.io": "^2.3.0",
    "socket.io-client": "^2.3.0",
    "socketio-auth": "^0.1.1"
  },
  "devDependencies": {}
}
